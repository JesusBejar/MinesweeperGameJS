import { createBoard } from "./minesweeper.js"
// frontend display code

console.log(createBoard(2, 2))
// Populate the board with tiles/mines

// add left (reveal tiles) & right click (mark tiles) functionality

// win/lose check